FROM node:18-alpine

WORKDIR /app

# Install dependencies (including devDependencies for type safety)
COPY package.json package-lock.json* ./
RUN npm ci --include=dev

# Copy application code
COPY . .

# Development mode - mount volume for hot reload
EXPOSE 3000

CMD ["npm", "run", "dev"]