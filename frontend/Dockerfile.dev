FROM node:18-alpine

WORKDIR /app

# Install dependencies
COPY package.json package-lock.json* ./
RUN npm ci

# Copy application code
COPY . .

# Development mode - mount volume for hot reload
EXPOSE 3000

CMD ["npm", "run", "dev"]